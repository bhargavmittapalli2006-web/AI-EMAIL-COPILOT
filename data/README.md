# Data Directory

This folder is reserved for datasets used in training and evaluating the AI models.

- `raw/`: Unprocessed email datasets.
- `processed/`: Processed feature matrices and cleaned data.

*Note: Raw data files are excluded from Git version control via `.gitignore`.*
